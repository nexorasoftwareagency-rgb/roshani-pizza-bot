# DEPRECATED

This directory is deprecated and no longer maintained.

The QR Ordering functionality has been integrated into the main Menu app (`menu/`).

**Do not deploy from this directory.** Use `menu/` for all QR ordering features.
