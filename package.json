{
  "scripts": {
    "build": "node tools/build.mjs",
    "deploy:admin": "npm run build && firebase deploy --only hosting:admin"
  },
  "dependencies": {
    "playwright": "^1.61.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "purgecss": "^8.0.0"
  }
}
