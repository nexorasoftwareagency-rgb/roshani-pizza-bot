# 🚀 Deployment Guide: Roshani Pizza ERP

This guide provides the necessary steps to sync and deploy the latest stability fixes (Layout & Timezone) to your production server.

## 1. Update Code from GitHub
First, log in to your server via SSH and navigate to the project directory:

```bash
cd /path/to/Prasant-Pizza-ERP
git pull origin main
```

---

## 2. Deploy WhatsApp Bot (Local Server / VPS)
The bot logic (Timezone/IST fixes) runs via Node.js. If you are using **PM2** (Process Manager), follow these steps:

```bash
cd bot
# Install any new dependencies
npm install
# Restart the bot process
pm2 restart all
# View logs to ensure bot is online and showing IST time
pm2 logs
```

---

## 3. Deploy Admin Portal (Firebase Hosting)
The Admin Portal layout fixes are deployed to Firebase. Navigate to the root or Admin folder:

```bash
# From the root directory
firebase deploy --only hosting
```

---

## ⚡ Critical Fixes Included (v3.1.0)
- **Timezone Sync**: Bot now operates on **Asia/Kolkata (IST)**. No more "Closed" messages during business hours.
- **Layout Stabilization**: Admin dashboard is now top-anchored and sidebar overlap is resolved.
- **Asset Integrity**: Broken local assets replaced with stable cloud placeholders.

---
_Generated by Antigravity AI_
